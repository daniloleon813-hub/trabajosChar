using System;

class Program
{
    static void Main(string[] args)
    {
        int x = 5;

        if (x > 3)
        {
            Console.WriteLine("x is 5, if returns true because x is greater than 3");
        }
    }
}
